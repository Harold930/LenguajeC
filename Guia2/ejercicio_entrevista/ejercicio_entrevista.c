#include  <stdio.h>
#include <stdlib.h>

typedef enum {
    FALSE,
    TRUE
} bool_t;

bool_t two_sum(int v[], size_t l, int target); /*esta es la declaración de la función */

/*La idea del ejerciciop es que nos dan un array y el tamaño del mi9smo, luego, tenemos qeu ver si dos numeros de
array suamdos dan el target, que es otro parametro que ops pasan a la función, entonces devolvemos TRUE, y en caso de que 
no encuentre el par numerico, entonces devuelve FALSE*/

int main(void){


}